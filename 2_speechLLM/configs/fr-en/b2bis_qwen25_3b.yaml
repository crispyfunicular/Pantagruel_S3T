# speechLLM B2bis — fr-en, Qwen2.5-3B-Instruct (projecteur à réentraîner).
# Prérequis : datasets/manifests_sentence/fr-en/ et processed_sentence/.
# Référence B1 Phi-2 : run_002_speechllm_b1_sentence_long (BLEU dev 19.99 / test 15.89).

experiment:
  name: "fr-en_speechllm_b2bis_qwen25_3b"
  lang_pair: "fr-en"
  output_dir: "runs/fr-en/run_010_speechllm_b2bis_qwen25_3b"
  seed: 42
  deterministic: true

data:
  train_manifest: "datasets/manifests_sentence/fr-en/train.tsv"
  valid_manifest: "datasets/manifests_sentence/fr-en/valid.tsv"
  test_manifest: "datasets/manifests_sentence/fr-en/test.tsv"
  sample_rate: 16000

model:
  encoder_name: "PantagrueLLM/speech-base-1K"
  trust_remote_code: true
  llm_name: "Qwen/Qwen2.5-3B-Instruct"
  llm_trust_remote_code: true
  downsample_k: 5
  projector_hidden: 2048
  freeze_encoder: true
  freeze_llm: true
  load_in_4bit: false
  load_in_8bit: false

prompt:
  format: "qwen_chatml"
  template: "Translate the French speech to English."

train:
  max_updates: 20000
  warmup_updates: 1000
  learning_rate_peak: 0.0001
  weight_decay: 0.0
  batch_size: 2
  gradient_accumulation: 4
  gradient_clip_norm: 1.0
  amp_dtype: "fp16"
  eval_every_updates: 1000
  max_eval_batches: 20
  max_target_tokens: 256

decode:
  beam_size: 1
  max_new_tokens: 48
