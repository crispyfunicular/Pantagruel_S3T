# S3T — dépendances runtime + développement (PRD.md, AGENTS.md)
# Python 3.10+
#
# PyTorch : pour le GPU, installer la build CUDA adaptée si besoin, par ex. :
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# (voir aussi scripts/bootstrap.sh --with-cuda-index-url)

# --- Runtime (pipeline 0…6) ---
torch>=2.0
torchaudio>=2.0
soundfile>=0.12
pyyaml>=6.0
timm>=0.9.0
transformers>=4.36
sacrebleu>=2.3
sentencepiece>=0.1.99
tensorboard>=2.14

# --- Développement (lint, tests, hooks ; inutiles sur une machine d'inférence seule) ---
ruff>=0.8.0
pytest>=8.0.0
pre-commit>=4.0.0
